# Local development only: publish ports so the stack is reachable from the host.
services:
  hapi:
    ports: ["8080:8080"]
  api:
    ports: ["3001:3000"]
    environment:
      PUBLIC_FHIR_URL: http://localhost:8080/fhir
