# Public URL of the backend API (Caddy on the EC2 box)
NEXT_PUBLIC_API_URL=https://oneaquahealth.duckdns.org

# Set to 1 to use the in-browser mock API (sample OAH sites, clinics and alerts) instead
NEXT_PUBLIC_API_MOCK=
